---
// Redireciona a raiz para o locale default. Astro com prefixDefaultLocale:true
// não cria esse redirect automaticamente em todos os cenários (dev serve o
// conteúdo vazio desta página, build gera index.html vazio).
return Astro.redirect("/pt/");
---
